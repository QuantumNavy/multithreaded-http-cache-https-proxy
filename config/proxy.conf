# Proxy Server Configuration File

# Network Settings
listen_address = 0.0.0.0
port = 8080

# Performance Settings
max_clients = 100
buffer_size = 8192
cache_capacity = 50
tunnel_timeout = 300000

# File Paths
blocked_domains_file = config/domainfilter.txt
log_file = logs/proxy.log
cache_dir = cache
